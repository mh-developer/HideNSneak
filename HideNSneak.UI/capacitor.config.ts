import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'web.app.hidensneak',
  appName: 'HideNSneak.UI',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
